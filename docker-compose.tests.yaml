services:
  test-runner:
    build:
      context: ./test_runner
      dockerfile: Dockerfile
    volumes:
      - ./src:/libraries/src
      - ./tests:/sketch/tests
    stdin_open: true
    tty: true
